export enum FormType {
    COMPETITION = "COMPETITION",
    SURVEY = "SURVEY",
}

export enum AccessType {
    PRIVATE = "PRIVATE",
    PUBLIC = "PUBLIC",
}

export enum FormStatus {
    VALID = "valid",
    INVALID = "invalid",
}
